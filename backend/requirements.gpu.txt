# ──────────────────────────────────────────────────────────────
# requirements.gpu.txt — GPU-accelerated dependencies
# ──────────────────────────────────────────────────────────────
# Mirror of requirements.txt with GPU substitutions:
#   - onnxruntime       → onnxruntime-gpu  (requires CUDA 12.x + cuDNN 9.x)
#   - torch+cpu         → torch+cu124       (CUDA 12.4 wheels)
#   - torchvision+cpu   → torchvision+cu124
#
# Tested with: RTX 3060, Driver 566.36, CUDA toolkit ≤12.6
# ──────────────────────────────────────────────────────────────

# ── Web framework ──
fastapi==0.104.1
uvicorn==0.24.0

# ── Database ──
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# ── Data validation ──
pydantic==2.5.0
pydantic-settings==2.1.0

# ── Auth ──
python-jose==3.3.0
passlib==1.7.4
bcrypt==4.1.1
python-multipart==0.0.6

# ── Messaging ──
paho-mqtt==1.6.1

# ── Scheduling ──
apscheduler==3.10.4

# ── Cache ──
redis==5.0.1

# ── YOLO / ML (GPU-accelerated) ──
ultralytics>=8.3.0,<9

onnx>=1.15.0

# onnxruntime-gpu 1.19+ defaults to CUDA 12.x + cuDNN 9.x.
# Pin to 1.20.x for best stability with CUDA 12.4 base image.
# Versions <1.19 ship CUDA 11 / cuDNN 8 which would conflict.
onnxruntime-gpu==1.20.1

# PyTorch CUDA 12.4 wheels — compatible with driver ≥525 (your 566.36 is fine).
# Pin to 2.5.1+cu124: well-tested stable release for CUDA 12.4.
# Avoid unpinned versions that could pull cu126 wheels (needs driver 570+).
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.5.1+cu124
torchvision==0.20.1+cu124

# ── Image processing ──
opencv-python==4.8.1.78
numpy==1.24.3
pillow==10.1.0

# ── Utilities ──
requests==2.31.0
python-dotenv==1.0.0

# ── Testing ──
pytest==7.4.3
httpx==0.25.2

# ── Export ──
openpyxl==3.1.5
